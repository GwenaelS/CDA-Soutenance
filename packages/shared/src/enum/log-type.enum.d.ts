export declare enum LogType {
    WARN = "warn",
    UNWARN = "unwarn",
    TIMEOUT = "timeout",
    UNTIMEOUT = "untimeout",
    KICK = "kick",
    BAN = "ban",
    UNBAN = "unban",
    CLEAR = "clear",
    LOCK = "lock",
    UNLOCK = "unlock"
}
