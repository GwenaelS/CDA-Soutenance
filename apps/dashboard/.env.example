# URL du backend NestJS (apps/api). Le front redirige vers ${VITE_API_URL}/auth/discord
# pour démarrer le flux OAuth2 Discord — il ne construit jamais l'URL Discord lui-même.
VITE_API_URL=http://localhost:3000
